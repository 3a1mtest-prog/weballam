// Type declaration for the plain-JS build helper, so vite.config.ts can import
// it (the script itself stays .mjs because plain `node` runs it after the build).
export declare function siteUrl(): string;
